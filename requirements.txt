# Web Framework
Django==6.0.7
djangorestframework==3.17.1
django-cors-headers==4.9.0

# Environment Variables
python-decouple==3.8

# Static Files (production)
whitenoise==6.12.0

# Reports
reportlab==5.0.0
openpyxl==3.1.5

# PostgreSQL (Neon DB — required for Vercel production)
psycopg2-binary==2.9.10
dj-database-url==2.3.0

# Note: Pillow excluded due to Python 3.14 compatibility issues.
# Note: gunicorn removed — not needed for Vercel serverless deployment.
